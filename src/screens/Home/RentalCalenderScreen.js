import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const RentalCalenderScreen = () => {
  return <Text style={{ fontSize: 48 }}>RentalCalenderScreen</Text>;
};

const styles = StyleSheet.create({});

export default RentalCalenderScreen;